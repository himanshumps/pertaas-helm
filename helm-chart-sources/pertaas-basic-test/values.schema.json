{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "properties": {
      "jobId": {
        "description": "The job id with shoule be used to run the test",
        "type": "string"
      },
      "requestJson": {
        "description": "The input json based on which the test will be executed",
        "type": "string"
      },
      "stepDuration": {
        "description": "The scrape interval for the running job. Default value is 10",
        "type": "integer",
        "minimum": 1
      },
      "cpu": {
        "description": " The limit on the amount of CPU to be used. This value is in millicores",
        "type": "integer",
        "minimum": 1
      },
      "memory": {
        "description": "The limit on the amount of memory to be used. This value is in MB.",
        "type": "integer",
        "minimum": 1
      }
    },
    "required": [
      "jobId",
      "stepDuration",
      "requestJson",
      "cpu",
      "memory"
    ],
    "title": "Values for running the pertaas-basic-test",
    "type": "object"
  }